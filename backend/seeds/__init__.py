"""Seeds dédiés V4 (Centre d'Action) — Sprint M2-4.x.

Séparé du package legacy `services/demo_seed/` : les seeds V4 ciblent les
8 tables introduites par la migration `m2s2v4` et restent opt-in (non branchés
dans l'orchestrateur HELIOS par défaut).
"""
